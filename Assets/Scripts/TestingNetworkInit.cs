using Unity.Netcode;
using UnityEngine;

public class TestingNetworkInit : MonoBehaviour
{
    [SerializeField]
    public NetworkManager network;
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
